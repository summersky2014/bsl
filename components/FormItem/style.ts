import { StyleSheet } from 'aphrodite/no-important';

const styles = StyleSheet.create({
  prompt: {
    width: 20,
    height: 20,
    background: '#ffe2e2',
    fill: '#ff0000',
    borderRadius: '50%',
    cursor: 'pointer'
  },
  pormptBox: {
    paddingLeft: 12
  }
});

export default styles;